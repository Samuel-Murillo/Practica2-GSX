# GreenDevCorp Backend — Python dependencies
# Versiones fijadas (pinned) para reproducibilidad y seguridad

# Framework web async — alto rendimiento para APIs
fastapi==0.111.1

# Servidor ASGI de producción — más eficiente que Flask dev server
uvicorn[standard]==0.30.1

# HTTP client async (para comunicación entre servicios)
httpx==0.27.0

# Validación de datos (incluida en FastAPI, versión explícita para seguridad)
pydantic==2.7.4

# Variables de entorno desde fichero .env (sólo para desarrollo local)
python-dotenv==1.0.1
